@page "/"
@inject NavigationManager Navigation
@rendermode @(new InteractiveServerRenderMode(prerender: false))
@attribute [Authorize]

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/orders"); // 例：ordersページへ遷移
    }
}